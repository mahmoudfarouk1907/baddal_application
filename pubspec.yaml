name: baddal_application
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  
  # المكتبة السحرية عشان الـ Splash Screens وميتقرروش تاني
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # تعريف فولدر الصور بشكل مظبوط ومسافات صحيحة لقرائتها
  assets:
    - assets/